- name: List repository contents
  run: ls -la

- name: Build and run tests
  run: |
    mkdir -p build
    cd build
    # Adjust this path according to where CMakeLists.txt actually is
    cmake .
    make
    cd ..
    ./full-test.sh
